node_modules/
.env
.DS_Store
dist/
client/node_modules/
client/dist/

# Nitro/Vinxi build output (client/.gitignore covers these for its own subtree)
.output/
.nitro/
.tanstack/
.vinxi/
.wrangler/

# Crash artifacts from Git-Bash on Windows; never source.
*.stackdump

# Separate repo (github.com/Alfaeran/reflex-clash-studio); was a broken gitlink
# with no .gitmodules, so a fresh clone got an empty directory.
reflex-clash-studio/
